{
  "name": "engineering-intelligence-platform",
  "version": "0.0.0",
  "private": true,
  "description": "An extensible platform that transforms engineering knowledge into actionable intelligence.",
  "scripts": {
    "format": "prettier --write '**/*.md' '*.json' '*.jsonc'",
    "format:check": "prettier --check '**/*.md' '*.json' '*.jsonc'",
    "lint:md": "markdownlint-cli2",
    "check": "npm run format:check && npm run lint:md",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "husky": "^9.1.7",
    "markdownlint-cli2": "^0.23.2",
    "prettier": "^3.5.3"
  }
}
